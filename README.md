# SSH SOCKS Proxy

Android app that serves as a SOCKS5 proxy over SSH.

## Features

- SOCKS5 proxy over SSH
- Connect using username and password authentication
- Connect using a private key
- Support for jump hosts
